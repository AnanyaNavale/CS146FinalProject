using UnityEngine;

public class PlayerInventory : MonoBehaviour
{
    // These start as false because we haven't found anything yet!
    public bool hasCasebook = false;
    public bool hasRubbing = false;
    public bool hasClock = false;
}